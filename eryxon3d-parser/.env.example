# =============================================================================
# CAD Processing Service - Environment Configuration
# =============================================================================

# Authentication
# --------------
# Comma-separated list of valid API keys
# Generate with: openssl rand -hex 32
API_KEYS=your-api-key-here,another-key-here

# Set to "false" to disable authentication (development only!)
REQUIRE_AUTH=true

# CORS Configuration
# ------------------
# Comma-separated list of allowed origins
# Use "*" to allow all origins (not recommended for production)
ALLOWED_ORIGINS=https://your-app.com,https://app.example.com

# File Processing
# ---------------
# Maximum file size in megabytes
MAX_FILE_SIZE_MB=100

# Service Configuration
# ---------------------
# Port for the service (inside container)
PORT=8000

# Enable Swagger/OpenAPI documentation
ENABLE_DOCS=true
