# Canny secret API key — SERVER-SIDE ONLY. Never commit the real value.
# Used at build time by src/lib/canny.ts to PULL the public roadmap (read-only).
# Get it from Canny admin settings > API. Leave blank in CI without secrets —
# the roadmap falls back to an honest empty state and the build stays green.
CANNY_API_KEY=
