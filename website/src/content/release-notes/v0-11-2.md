---
title: "API contract and deployment cleanup"
version: "v0.11.2"
date: 2026-09-19
status: "stable"
summary: "The public REST contract now matches the shipped Edge Functions, batch writes reject internal fields, and obsolete hosted endpoints have been removed."
ctaIntent: "docs"
items:
  - type: fixed
    title: "One current REST contract"
    body: "The OpenAPI document and payload guide now describe jobs, parts, operations, batches, webhooks and private files with the field names and filters used by the running API. Removed task and stage routes no longer appear."
  - type: fixed
    title: "Batch writes stay within their contract"
    body: "Batch create and update payloads now reject lifecycle, calculated and ownership fields. Start, stop and operation assignment continue through the dedicated lifecycle endpoint."
  - type: fixed
    title: "NCR values match the database"
    body: "Category and disposition validation uses the current database values, so invalid legacy values are rejected before a write is attempted."
  - type: changed
    title: "Hosted functions match the repository"
    body: "Old test, task, stage, MQTT, billing and webhook-log functions have been removed from the hosted project. Only functions shipped from the public repository remain deployed."
  - type: fixed
    title: "Portable scheduled evaluation"
    body: "The pilot evaluator reads its deployment URL from Supabase Vault instead of carrying a hosted project address in source. Repository agent configuration no longer connects a clone to that project."
docsLinks:
  - label: "REST API reference"
    href: "/api/rest-api-reference/"
  - label: "API payload reference"
    href: "/api/payload-reference/"
  - label: "Webhooks"
    href: "/architecture/connectivity-webhooks/"
githubUrl: "https://github.com/SheetMetalConnect/eryxon-flow/releases/tag/v0.11.2"
---

This patch follows the hosted deployment audit for 0.11.1. API consumers do not
need a migration; integrations using retired endpoints must move to the current
operation and webhook APIs.
