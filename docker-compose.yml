# Eryxon Flow - Self-Hosting Deployment
#
# Quick start:
#   1. Copy .env.example to .env and fill in your values
#   2. docker compose up -d
#
# For HTTPS with automatic SSL:
#   1. Uncomment the caddy service below
#   2. Edit Caddyfile for your hostname
#   3. Change eryxon-flow ports from "80:80" to expose: ["80"]

services:
  eryxon-flow:
    image: ${ERYXON_IMAGE:-ghcr.io/sheetmetalconnect/eryxon-flow:0.11.0}
    container_name: eryxon-flow
    restart: unless-stopped
    ports:
      - "80:80"
    env_file: .env
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://127.0.0.1/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  # Optional: Caddy reverse proxy for automatic HTTPS (recommended for production)
  #
  # To enable:
  #   1. Uncomment the caddy service and volumes below
  #   2. Change eryxon-flow from ports: ["80:80"] to expose: ["80"]
  #   3. Edit Caddyfile (section A for a public hostname, B for LAN)
  #
  # caddy:
  #   image: caddy:alpine
  #   container_name: caddy
  #   restart: unless-stopped
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ./Caddyfile:/etc/caddy/Caddyfile:ro
  #     - caddy_data:/data
  #     - caddy_config:/config
  #   depends_on:
  #     - eryxon-flow

# volumes:
#   caddy_data:
#   caddy_config:
