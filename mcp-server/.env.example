# =============================================================================
# Eryxon Flow MCP Server Configuration
# =============================================================================

# Choose your deployment mode:
# - DIRECT: Self-hosted with service key (single tenant, direct database access)
# - API: Cloud SaaS with API keys (multi-tenant, via REST API)

# -----------------------------------------------------------------------------
# OPTION 1: Direct Mode (Self-Hosted)
# -----------------------------------------------------------------------------
# Use this for self-hosted single-tenant deployments
# Requires service role key for direct Supabase access

# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_SERVICE_KEY=eyJhbGc...

# -----------------------------------------------------------------------------
# OPTION 2: API Mode (Cloud SaaS) - RECOMMENDED FOR HOSTED DEPLOYMENT
# -----------------------------------------------------------------------------
# Use this when deploying MCP server as SaaS service
# Users authenticate with their tenant-scoped API keys

ERYXON_API_URL=https://your-project.supabase.co
# ERYXON_API_KEY is provided by each user in their Claude Desktop config

# -----------------------------------------------------------------------------
# Optional Features
# -----------------------------------------------------------------------------

# Redis for caching (Upstash recommended for cloud)
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=

# -----------------------------------------------------------------------------
# Deployment Configuration (for hosted MCP service)
# -----------------------------------------------------------------------------

# Port for HTTP server
PORT=3000

# Allowed origins for CORS
ALLOWED_ORIGINS=https://app.eryxon.com,https://eryxon.com
